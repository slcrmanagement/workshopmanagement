import LoginClient from './LoginClient';

export const metadata = { title: 'Admin Login | SLCR Workshop Portal' };
export const dynamic = 'force-dynamic';

export default function AdminLoginPage() {
  return <LoginClient />;
}
